apiVersion: v1
kind: Service
metadata:
  name: whiskey-service
spec:
  selector:
    app: whiskey-tracker
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer